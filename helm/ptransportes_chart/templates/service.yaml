apiVersion: v1
kind: Service
metadata:
  name: {{ include "ptransportes_chart.fullname" . }}
  labels:
    {{- include "ptransportes_chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ptransportes_chart.selectorLabels" . | nindent 4 }}
